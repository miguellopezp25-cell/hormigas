import { ProductLayoutClient } from "@/components/product-layout-client";

export default function FormicariaLayout({ children }: { children: React.ReactNode }) {
  return <ProductLayoutClient>{children}</ProductLayoutClient>;
}
